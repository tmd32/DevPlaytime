<Window x:Class="DevPlaytimeDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DevPlaytime"
        Width="1240"
        Height="800"
        MinWidth="1000"
        MinHeight="650"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Icon="pack://application:,,,/Assets/DevPlaytime.ico"
        Background="{StaticResource AppBackground}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        Closing="Window_Closing">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource AppBackground}" BorderBrush="#1A1E28" BorderThickness="0,0,0,1">
            <Grid x:Name="TitleBar" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                    <Viewbox Width="22" Height="22" Stretch="Uniform" Margin="0,0,9,0">
                        <Border Width="27" Height="27" CornerRadius="8" Background="#12352F" BorderBrush="#56E0B0" BorderThickness="1" Padding="5">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                                <Border Width="3" Height="8" Background="#56E0B0" CornerRadius="2" Margin="0,0,3,0" />
                                <Border Width="3" Height="14" Background="#56E0B0" CornerRadius="2" Margin="0,0,3,0" />
                                <Border Width="3" Height="19" Background="#56E0B0" CornerRadius="2" />
                            </StackPanel>
                        </Border>
                    </Viewbox>
                    <TextBlock Text="DevPlaytime" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <TextBlock x:Name="LocalTrackerText" Text="  ·  LOCAL TRACKER" Foreground="{StaticResource SubtleBrush}" FontFamily="Consolas" FontSize="9" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Button Content="—" Style="{StaticResource ChromeButton}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Minimize_Click" />
                    <Button x:Name="MaximizeButton" Content="□" Style="{StaticResource ChromeButton}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Maximize_Click" />
                    <Button Content="×" Style="{StaticResource ChromeButton}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#090B11" BorderBrush="#1A1E28" BorderThickness="0,0,1,0">
            <DockPanel Margin="22,30,22,22">
                <StackPanel DockPanel.Dock="Bottom">
                    <Border Background="#152A2A" BorderBrush="#21473F" BorderThickness="1" CornerRadius="13" Padding="15" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="7" Height="7" Fill="{StaticResource MintBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock x:Name="AutoTrackingTitle" Text="Automatic tracking active" FontSize="11" FontWeight="Bold" Foreground="#B8F6DF" />
                            </StackPanel>
                            <TextBlock x:Name="AutoTrackingDescription" Text="Work sessions are recorded&#x0a;as soon as a program starts." Foreground="{StaticResource MutedBrush}" FontSize="11" LineHeight="18" Margin="0,9,0,0" />
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="VersionText" Text="LOCAL ONLY  ·  v0.5.0" Foreground="#454B58" FontSize="10" FontFamily="Consolas" Margin="10,0,0,0" />
                </StackPanel>

                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="9,0,0,49">
                        <Border Width="27" Height="27" CornerRadius="8" Background="#12352F" BorderBrush="#56E0B0" BorderThickness="1" Padding="5" Margin="0,0,11,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                                <Border Width="3" Height="8" Background="#56E0B0" CornerRadius="2" Margin="0,0,3,0" />
                                <Border Width="3" Height="14" Background="#56E0B0" CornerRadius="2" Margin="0,0,3,0" />
                                <Border Width="3" Height="19" Background="#56E0B0" CornerRadius="2" />
                            </StackPanel>
                        </Border>
                        <TextBlock Text="DEV" FontSize="14" FontWeight="ExtraBold" VerticalAlignment="Center" />
                        <TextBlock Text="PLAYTIME" FontSize="14" FontWeight="ExtraBold" Foreground="{StaticResource MintBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="WorkspaceText" Text="WORKSPACE" Foreground="{StaticResource SubtleBrush}" FontFamily="Consolas" FontSize="10" Margin="12,0,0,13" />
                    <Button x:Name="LibraryNav" Tag="library" Content="▦   Library" Style="{StaticResource FlatButton}" HorizontalContentAlignment="Left" FontSize="14" Click="Nav_Click" Margin="0,0,0,4" />
                    <Button x:Name="TimelineNav" Tag="timeline" Content="◷   Timeline" Style="{StaticResource FlatButton}" HorizontalContentAlignment="Left" FontSize="14" Click="Nav_Click" Margin="0,0,0,4" />
                    <Button x:Name="SettingsNav" Tag="settings" Content="⚙   Settings" Style="{StaticResource FlatButton}" HorizontalContentAlignment="Left" FontSize="14" Click="Nav_Click" />
                </StackPanel>
            </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="48,28,42,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <StackPanel>
                    <TextBlock x:Name="EyebrowText" Text="LOCAL WORKSPACE" Foreground="{StaticResource SubtleBrush}" FontFamily="Consolas" FontSize="10" />
                    <TextBlock x:Name="PageTitle" Text="My work library" FontSize="38" FontWeight="ExtraBold" Margin="0,10,0,0" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,22,0">
                        <Ellipse x:Name="ScanDot" Width="7" Height="7" Fill="{StaticResource MintBrush}" Margin="0,0,9,0" />
                        <TextBlock x:Name="ScanStatus" Text="Preparing program detection" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                    </StackPanel>
                    <Button x:Name="AddProgramButton" Content="＋ Add program" Style="{StaticResource PrimaryButton}" Click="AddApp_Click" />
                </StackPanel>
            </Grid>

            <UniformGrid x:Name="SummaryGrid" Grid.Row="1" Columns="4" Margin="0,30,0,28" />
            <ContentControl x:Name="ContentHost" Grid.Row="2" />
            <TextBlock x:Name="PrivacyText" Grid.Row="3" Text="DevPlaytime does not inspect your work. It quietly remembers how long programs stayed open." Foreground="#454B58" FontSize="10" HorizontalAlignment="Center" Margin="0,28,0,0" />
            </Grid>
        </Grid>
    </Grid>
</Window>
